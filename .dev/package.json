{
  "name": "health-plan-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.js",
    "generate-llms": "node scripts/generate-llms.js",
    "dev": "npm run sync && npm run generate-llms && astro dev",
    "build": "npm run sync && npm run generate-llms && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/sitemap": "^3.7.0",
    "@astrojs/starlight": "^0.37.6",
    "astro": "^5.2.0",
    "markdown-it": "^14.1.1"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
