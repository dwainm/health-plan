{
  "scripts": {
    "build": "cd .dev && npm install && npm run build",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
